extends layout

block content
  h1= title
  ul
    li
      a.active(href='/') All Unit
    li
      a(href='/addUnit') Add Unit
  br
  each r in re
    h1 Unit: #{r.Unit}
    img(src=`/images/${r.Unit}.png`)
    a(href=`/dettagli/${r.Unit}`) Click for details of: #{r.Unit}
    br
    hr